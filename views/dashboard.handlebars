<section class="new-post-dash">
    <h2 class="create-header">Create New Post</h2>

    <form class="new-post-form">
        <div class="title-form-dash">
            <label class="form-label" for="post-title">Title</label>
            <input class="form-input" type="text" id="post-title" name="post-title" />
        </div>
        <div>
            <label class="form-label" for="post-text">Content</label>
            <input class="form-input" id="post-text" name="post-text" />
        </div>
        <button type="submit" class="btn create-button">Create</button>
    </form>
</section>

{{#if posts.length}}
<section>
    <h2>Your Posts</h2>
    <ol>
        {{#each posts as |post|}}
        <li>
            {{> post-info post}}
            <div id="edit-contianer">

            <a href="/dashboard/edit/{{post.id}}" id="dashboard">Edit post</a>
            </div>
        </li>
        {{/each}}
    </ol>
</section>
{{/if}}

<script src="/javascript/add-post.js"></script>